import ko from "knockout";
import ViewModel from "../../ko/ViewModel";

class SelectField extends ViewModel {
  constructor(...args) {
    super(...args);
  }
}

export default SelectField;
